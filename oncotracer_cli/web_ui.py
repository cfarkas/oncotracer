"""Self-contained browser UI; no external scripts, fonts, or other network assets."""

PAGE = r'''<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>OncoTracer · Analysis setup</title>
<style>
:root{color-scheme:light;--ink:#192c3b;--muted:#5b6d78;--line:#dce4e6;--paper:#fff;--accent:#116f68;--wash:#edf5f3}*{box-sizing:border-box}body{margin:0;background:#f3f5f3;color:var(--ink);font:16px/1.55 system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}header{border-bottom:1px solid var(--line);background:var(--paper);padding:18px max(24px,calc((100vw - 1100px)/2));display:flex;align-items:center;justify-content:space-between;gap:24px}.brand{font-size:21px;font-weight:750;letter-spacing:-.6px}.brand span{color:var(--accent)}.local{font-size:12px;text-transform:uppercase;letter-spacing:1px;color:var(--muted)}main{max-width:1100px;margin:40px auto 80px;padding:0 24px}h1{font-size:36px;line-height:1.2;letter-spacing:-1px;margin:12px 0}h2{font-size:23px;letter-spacing:-.4px;margin:0}h3{font-size:17px}p{margin:8px 0 18px}.muted,.hint{color:var(--muted)}.hint{font-size:13px}.eyebrow{font-size:12px;letter-spacing:1.5px;text-transform:uppercase;color:var(--accent);font-weight:700}.intro{max-width:710px;margin-bottom:30px}.card{background:var(--paper);border:1px solid var(--line);border-radius:13px;padding:28px;margin:20px 0;box-shadow:0 3px 8px #192c3b04}.sectionhead{display:flex;align-items:center;gap:14px;margin-bottom:18px}.step{background:var(--wash);color:var(--accent);width:34px;height:34px;border-radius:50%;display:grid;place-items:center;font-size:14px;font-weight:750;flex:none}.grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:20px}.platform{background:#fff;border:1px solid var(--line);text-align:left;white-space:normal;padding:23px;color:var(--ink);min-height:130px;cursor:pointer;border-radius:10px}.platform strong{font-size:22px;display:block;margin:0 0 8px}.platform span{font-size:14px;color:var(--muted);display:block}.platform[aria-pressed=true]{border:2px solid var(--accent);background:var(--wash);padding:22px}.platform:hover{border-color:var(--accent)}label{display:block;font-size:14px;font-weight:600;margin:0 0 7px}input,select,button{font:inherit}input:not([type=checkbox]),select{width:100%;border:1px solid #c4d1d5;border-radius:6px;padding:10px 11px;background:white;color:var(--ink);min-width:0}input[type=checkbox]{width:18px;height:18px;accent-color:var(--accent);vertical-align:middle}button{border:1px solid #c4d1d5;border-radius:6px;padding:10px 16px;cursor:pointer;background:#fff;color:var(--ink);font-weight:650;white-space:nowrap}button:hover{background:var(--wash)}button.primary{background:var(--accent);color:white;border-color:var(--accent)}button.primary:hover{background:#085b55}a.result-link{display:inline-block;border-radius:6px;padding:10px 16px;background:var(--accent);color:white;font-weight:650;text-decoration:none}a.result-link:hover{background:#085b55}button:disabled{opacity:.5;cursor:wait}button:focus-visible,input:focus-visible,select:focus-visible{outline:3px solid #65bcb2;outline-offset:2px}.row{display:flex;gap:10px;align-items:center}.row input{flex:1}.actions{display:flex;gap:12px;align-items:center;flex-wrap:wrap;margin-top:22px}.hardware{display:flex;gap:24px;flex-wrap:wrap;background:#f6f8f7;border-radius:8px;padding:16px;margin:15px 0 24px}.hardware strong{display:block;font-size:20px}.hardware span{font-size:12px;color:var(--muted)}.notice{background:var(--wash);border-left:3px solid var(--accent);padding:12px 16px;font-size:14px;margin:16px 0}.error{background:#fff0ee;color:#8b2922;border:1px solid #edc5bf;border-radius:8px;padding:14px 18px;margin:16px 0;white-space:pre-wrap;overflow-wrap:anywhere}.samplewrap{overflow-x:auto}table{width:100%;border-collapse:collapse;font-size:14px}th{text-align:left;color:var(--muted);font-size:12px;text-transform:uppercase;letter-spacing:.6px;padding:10px 8px;border-bottom:1px solid var(--line)}td{vertical-align:top;padding:14px 8px;border-bottom:1px solid var(--line)}td:first-child{width:32px}td.name{min-width:185px}td.type{min-width:180px}td small{display:block;color:var(--muted);margin-top:5px}summary{cursor:pointer;font-size:13px;color:var(--accent)}details{margin:12px 0}details pre{font-size:12px}pre{white-space:pre-wrap;overflow-wrap:anywhere;font:13px/1.5 ui-monospace,SFMono-Regular,Consolas,monospace;background:#f5f7f8;border-radius:6px;padding:16px;max-height:420px;overflow:auto}.logs{background:#14242d;color:#dce9e5;min-height:180px;max-height:540px}.badge{display:inline-block;border-radius:30px;padding:4px 12px;background:var(--wash);color:var(--accent);font-size:13px;font-weight:650}.field{margin-bottom:18px}.check{font-weight:400;margin:14px 0}.check input{margin-right:8px}dialog{width:min(700px,calc(100vw - 28px));border:1px solid var(--line);border-radius:12px;padding:26px;color:var(--ink);box-shadow:0 20px 80px #0003}dialog::backdrop{background:#12283188}.folders{height:310px;overflow:auto;border:1px solid var(--line);border-radius:6px;margin-top:12px}.folder{display:block;width:100%;border:0;border-bottom:1px solid var(--line);border-radius:0;text-align:left;font-weight:400;overflow-wrap:anywhere;white-space:normal}.folder:before{content:'▸';margin-right:12px;color:var(--accent)}.folderpath{font-family:ui-monospace,monospace;font-size:12px;overflow-wrap:anywhere;margin:12px 0}[hidden]{display:none!important}footer{font-size:13px;color:var(--muted);margin-top:26px}.progress{margin-left:auto;color:var(--muted);font-size:13px}@media(max-width:720px){main{padding:0 14px;margin-top:24px}.card{padding:20px}.grid{grid-template-columns:1fr}.row{flex-wrap:wrap}.row input{flex-basis:100%}h1{font-size:29px}header{padding:16px}.local{font-size:10px}}

.sample-board{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:16px;margin-top:20px}.sample-column{border:1px solid var(--line);border-radius:10px;background:#f6f8f7;padding:14px;min-width:0}.sample-column h3{margin:0 0 10px}.sample-column[data-group=normal]{background:#eef5fc}.sample-column[data-group=cancer]{background:#fff3ef}.sample-column[data-group=custom]{grid-column:1/-1}.sample-list{min-height:190px}.sample-list:empty:after{content:attr(data-empty);display:block;padding:25px 8px;color:var(--muted);font-size:14px;text-align:center}.sample-column.drag-over{outline:3px solid var(--accent);outline-offset:2px}.sample{background:#fff;border:1px solid var(--line);border-radius:8px;padding:12px;margin-bottom:12px;box-shadow:0 2px 5px #192c3b08;overflow-wrap:anywhere}.sample.dragging{opacity:.45}.sample .sample-name{font-weight:650}.sample .type-select{margin-top:12px}.sample small{display:block;font-size:12px;color:var(--muted);margin:7px 0}.drag-handle{font-size:12px;color:var(--muted);cursor:grab;touch-action:none;user-select:none;margin-bottom:8px}.sample input,.sample select{cursor:auto}.sample .custom-label,.sample .role{margin-top:8px}@media(max-width:800px){.sample-board{grid-template-columns:1fr}.sample-list{min-height:100px}}
</style></head><body>
<header><div class="brand">Onco<span>Tracer</span></div><div class="local">Local analysis workspace</div></header>
<main><div class="intro"><h1>Configure and run an analysis</h1><p class="muted">Browse to your FASTQ folder, assign and name samples, and choose analysis settings. Save the configuration, start or stop analysis, follow its progress, and open the results here.</p></div>
<div id="error" role="alert" class="error" hidden></div>
<section class="card" id="platform-card"><div class="sectionhead"><span class="step">1</span><h2>Choose your sequencing platform</h2></div><div class="grid">
<button class="platform" id="choose-ont" aria-pressed="false"><strong>Oxford Nanopore</strong><span>Barcode folders with FASTQ batches, or one folder from a nonbarcoded ligation library.</span></button>
<button class="platform" id="choose-illumina" aria-pressed="false"><strong>Illumina</strong><span>Paired-end or single-end FASTQs. Sample names and read pairs are discovered for you.</span></button></div></section>
<div id="workflow" hidden>
<section class="card"><div class="sectionhead"><span class="step">2</span><h2>Find your FASTQ folder</h2></div><p class="muted">Browse folders on the computer running OncoTracer. Select the folder containing your FASTQs or barcode folders.</p><label for="input-folder">FASTQ folder</label><div class="row"><input id="input-folder" autocomplete="off" spellcheck="false"><button data-browse="input-folder">Browse folders</button><button class="primary" id="scan">Discover samples</button></div><p class="hint" id="layout-hint"></p><div id="scan-warnings"></div></section>
<section class="card" id="samples-card" hidden><div class="sectionhead"><span class="step">3</span><h2>Assign and name your samples</h2><span id="sample-count" class="progress" role="status" aria-live="polite"></span></div><p class="muted">Drag detected samples into <strong>Normal</strong> or <strong>Cancer</strong>, or use the type dropdown on each card. Drag between columns to correct a type, or back to Unassigned to exclude a sample. Edit the detected names as needed.</p><p class="hint">Normal samples are analyzed independently; they are not pooled or subtracted. A custom tag also needs a study or control role.</p><button id="select-none">Clear assignments</button><div id="samples" class="sample-board">
<div class="sample-column" data-group=""><h3>Detected · unassigned</h3><div id="available-samples" class="sample-list" data-empty="All detected samples have been assigned."></div></div>
<div class="sample-column" data-group="normal"><h3>Normal</h3><div id="normal-samples" class="sample-list" data-empty="Drop normal samples here."></div></div>
<div class="sample-column" data-group="cancer"><h3>Cancer</h3><div id="cancer-samples" class="sample-list" data-empty="Drop cancer samples here."></div></div>
<div class="sample-column" data-group="custom" id="custom-column" hidden><h3>Custom tags</h3><div id="custom-samples" class="sample-list"></div></div>
</div></section>
<section class="card" id="settings-card" hidden><div class="sectionhead"><span class="step">4</span><h2>Choose analysis settings</h2></div><div id="hardware" class="hardware"></div><p id="gpu-note" class="hint"></p>
<div class="grid"><div><label for="analysis">Analysis</label><select id="analysis"><option value="cna">Copy-number analysis (CNA)</option><option value="methylation">Methylation classification</option><option value="both">CNA and methylation</option></select></div><div><label for="threads">CPU threads</label><input id="threads" type="number" min="1" step="1"></div>
<div id="caller-field"><label for="caller">ONT CNA method</label><select id="caller"><option value="ichorcna">ichorCNA · 500 kb bins</option><option value="qdnaseq">QDNAseq · solid biopsy</option></select></div><div id="binsize-field"><label for="binsize">CNA bin size (kb)</label><select id="binsize"></select><p class="hint" id="binsize-default">Default: 100 kb for QDNAseq.</p></div></div><p id="controls-note" class="notice" hidden>Including normal/control samples selects the ONT solid-biopsy QDNAseq workflow.</p>
<label class="check" id="reports-field"><input id="reports" type="checkbox">Add CNA interpretation reports</label><div id="report-context-field" hidden><label for="report-context">Study context for reports</label><input id="report-context" value="broad_cancer"><div class="field"><label for="report-detail">Report detail</label><select id="report-detail"><option value="catalog">Local evidence catalog</option><option value="models">Local catalog and language-model drafts</option><option value="literature">Online literature and local language models</option></select></div><p id="report-detail-note" class="hint">Uses the included evidence catalog. No publication searches or language-model downloads.</p><label class="check"><input id="gistic" type="checkbox">Find recurrent CNAs across samples with GISTIC (at least two samples)</label><p id="gistic-note" class="hint" role="status"></p></div>
<div id="methylation-fields" hidden><h3>Methylation resources</h3><p class="hint">FASTQs alone do not retain modified-base calls. Supply an existing modified-base BAM or raw POD5 signal, plus a resource YAML with your installed tool/model paths.</p><div class="grid"><div><label for="classifier">Classifier</label><select id="classifier"><option value="">Choose a classifier…</option><option value="marlin">MARLIN · leukemia research</option><option value="sturgeon">Sturgeon · CNS-tumor research</option></select></div><div><label for="device">Methylation compute</label><select id="device"><option value="cpu">CPU</option><option value="gpu">GPU</option></select></div><div><label for="methylation-source">Input source</label><select id="methylation-source"><option value="modbam">Modified-base BAM (file or folder)</option><option value="pod5">Raw POD5 folder</option></select></div><div><label for="methylation-path">Input path</label><div class="row"><input id="methylation-path"><button data-browse="methylation-path" data-kind="any">Browse</button></div></div></div><label for="resources">Resource YAML path</label><div class="row"><input id="resources"><button data-browse="resources" data-kind="file">Browse YAML</button></div><label class="check"><input id="license" type="checkbox">For Sturgeon: I obtained it and accepted its applicable license.</label></div>
<div class="grid"><div><label for="backend">Analysis tools</label><select id="backend"><option value="conda">Conda · managed installation</option><option value="docker">Docker</option><option value="singularity">Apptainer / Singularity</option><option value="poetry">Poetry</option><option value="host">Existing tools on this computer</option></select></div><div><label for="reference">hg38 reference</label><select id="reference"><option value="download">Download prepared indexes when needed</option><option value="reuse">Reuse a prepared OncoTracer reference</option><option value="build">Build indexes locally on CPU</option></select></div></div><p id="reference-note" class="hint">Prepared downloads are reused. About 8.0 GiB for Illumina or 9.7 GiB for ONT, under your project’s reference folder.</p><div id="reference-path-field" hidden><label for="reference-path">Prepared reference parent or references/samurai_hg38 folder</label><div class="row"><input id="reference-path"><button data-browse="reference-path">Browse folders</button></div></div>
<h3>Save to a new project</h3><div class="grid"><div><label for="project-parent">Parent folder</label><div class="row"><input id="project-parent"><button data-browse="project-parent">Browse</button></div></div><div><label for="project-name">New project folder name</label><input id="project-name" value="oncotracer-analysis" spellcheck="false"></div></div><p class="hint">Configuration, sample metadata, logs, and results will be saved here. Existing configurations are protected.</p><div class="actions"><button class="primary" id="prepare">Save configuration and check</button><span class="hint">Analysis starts only when you choose Run below.</span></div></section>
<section class="card" id="review-card" hidden><div class="sectionhead"><span class="step">5</span><h2>Review and run</h2><span class="badge" id="check-badge"></span></div><p id="saved-path" class="folderpath"></p><div id="check-messages"></div><details><summary>View the saved configuration</summary><pre id="config-preview"></pre></details><p class="notice">Run analysis validates the saved settings, prepares missing analysis tools, and downloads reference files when needed. Processing can take time. Keep this terminal open to follow progress here.</p><div class="actions"><button id="run" class="primary">Run analysis</button><button id="stop" hidden>Stop analysis</button><span id="job-status" role="status"></span><a id="open-results" class="result-link" target="_blank" rel="noopener noreferrer" hidden>Open results</a><button id="new-analysis" hidden>Set up another analysis</button></div><p id="log-path" class="folderpath"></p><pre id="logs" class="logs" hidden></pre></section>
</div><footer>OncoTracer runs on this computer. This page loads no external assets and does not upload your files.</footer></main>
<dialog id="browser"><div class="sectionhead"><h2>Select a folder</h2><button id="close-browser" style="margin-left:auto" aria-label="Close folder navigator">Close</button></div><p class="hint">Folders on the computer running OncoTracer</p><div id="browser-error" class="error" role="alert" hidden></div><div class="row"><input id="browser-location" aria-label="Folder path"><button id="browser-go">Go</button><button id="browser-up">Up</button></div><p id="browser-path" class="folderpath"></p><div id="folders" class="folders"></div><p id="folder-summary" class="hint"></p><div class="actions"><button id="use-folder" class="primary">Use this folder</button></div></dialog>
<dialog id="cleanup"><h2>Analysis stopped</h2><p>Keep the project to resume later, or remove its folder?</p><p id="cleanup-path" class="folderpath"></p><p class="hint">Removal deletes this project's configuration, logs, reference folder and results. Original input files and shared download caches remain.</p><p id="cleanup-note" class="hint"></p><div id="remove-confirmation" hidden><label for="confirm-path">To permanently remove this folder, enter its complete path</label><input id="confirm-path" autocomplete="off" spellcheck="false"><div class="actions"><button id="confirm-remove" disabled>Permanently remove project folder</button></div></div><div class="actions"><button id="keep-project" class="primary" autofocus>Keep project</button><button id="remove-project">Remove project folder…</button></div></dialog>
<script>
'use strict';
const $=id=>document.getElementById(id), token=location.hash.slice(1);let mode=null,scan=null,prepared=null,activeJob=null,cleanupShown=null,folderTarget=null,currentFolder=null,parentFolder=null,hardware=null,pollTimer=null,browseKind="folder",binsizes=[1,5,10,15,30,50,100,500,1000],browseSequence=0,operationBusy=false,binsizeCaller=null;
const show=(id,yes)=>$(id).hidden=!yes;
function failure(error,id='error'){const node=$(id);node.textContent=error.message||String(error);node.hidden=false;if(id==='error')node.scrollIntoView({behavior:'smooth',block:'center'});}
async function api(path,data){let response;try{response=await fetch(path,{method:data?'POST':'GET',headers:{'X-OncoTracer-Token':token,...(data?{'Content-Type':'application/json'}:{})},...(data?{body:JSON.stringify(data)}:{})});}catch(error){throw Error('Cannot reach OncoTracer at '+location.origin+'. Keep the terminal running oncotracer setup open. If it stopped, run oncotracer setup again and open the NEW complete URL printed there, including the session code after #.');}const result=await response.json();if(!response.ok)throw Error(result.error||'Request failed.');return result;}
async function busy(button,action){if(operationBusy)return;operationBusy=true;button.disabled=true;document.querySelector('main').inert=true;show('error',false);try{await action();}catch(error){failure(error);}finally{button.disabled=false;operationBusy=false;document.querySelector('main').inert=false;}}
function node(tag,text,cls){const element=document.createElement(tag);if(text!==undefined)element.textContent=text;if(cls)element.className=cls;return element;}
function invalidate(){prepared=null;show('review-card',false);show('open-results',false);$('open-results').removeAttribute('href');}
function choose(platform){if(operationBusy)return;mode=platform;binsizeCaller=null;invalidate();scan=null;show('workflow',true);show('samples-card',false);show('settings-card',false);for(const name of ['ont','illumina'])$('choose-'+name).setAttribute('aria-pressed',String(name===mode));$('analysis').value='cna';for(const option of $('analysis').options)option.disabled=mode==='illumina'&&option.value!=='cna';$('layout-hint').textContent=mode==='ont'?'Each barcode folder is one sample, even with many FASTQs. A nonbarcoded ligation folder is one sample containing all its FASTQs.':'Illumina pairs are matched by their R1/R2 or _1/_2 filenames. Choose one library layout per project.';$('input-folder').focus();}
$('choose-ont').onclick=()=>choose('ont');$('choose-illumina').onclick=()=>choose('illumina');
async function browse(path){const sequence=++browseSequence;show('browser-error',false);$('use-folder').disabled=true;try{const result=await api('/api/browse?path='+encodeURIComponent(path||''));if(sequence!==browseSequence)return;currentFolder=result.path;parentFolder=result.parent;$('browser-location').value=result.path;$('browser-path').textContent=result.path;$('folders').replaceChildren();for(const dir of result.directories){const button=node('button',dir.name,'folder');button.onclick=()=>browse(dir.path);$('folders').append(button);}if(browseKind!=='folder')for(const file of result.files||[]){if(browseKind==='file'&&!/\.ya?ml$/i.test(file.name))continue;const button=node('button','File · '+file.name,'folder');button.onclick=()=>{$(folderTarget).value=file.path;$(folderTarget).dispatchEvent(new Event('change'));$('browser').close();};$('folders').append(button);}if(!result.directories.length&&!$('folders').children.length)$('folders').append(node('p','No subfolders here.','hint'));$('folder-summary').textContent=result.fastq_files+' FASTQ files in this folder'+(result.truncated?' · First 1,000 subfolders shown; enter a path to navigate further.':'');$('use-folder').disabled=browseKind==='file';}catch(error){if(sequence!==browseSequence)return;failure(error,'browser-error');$('use-folder').disabled=true;}}
for(const button of document.querySelectorAll('[data-browse]'))button.onclick=()=>{folderTarget=button.dataset.browse;browseKind=button.dataset.kind||'folder';$('browser').showModal();let path=$(folderTarget).value||$('project-parent').value;if(browseKind!=='folder'&&/\.(ya?ml|bam)$/i.test(path))path=path.slice(0,path.lastIndexOf('/'))||'/';browse(path);};
$('browser').addEventListener('close',()=>{browseSequence++;});$('close-browser').onclick=()=>$('browser').close();$('browser-go').onclick=()=>browse($('browser-location').value);$('browser-up').onclick=()=>browse(parentFolder);$('browser-location').onkeydown=event=>{if(event.key==='Enter')browse(event.target.value);};$('use-folder').onclick=()=>{$(folderTarget).value=currentFolder;$(folderTarget).dispatchEvent(new Event('change'));$('browser').close();};
function sampleRows(){return [...$('samples').querySelectorAll('.sample')].sort((a,b)=>Number(a.dataset.id)-Number(b.dataset.id));}
function selectedRows(){return sampleRows().filter(row=>row.querySelector('.include').checked);}
function moveSample(row,kind){
  if(operationBusy||$('prepare').disabled)return;
  row.querySelector('.type-select').value=kind;
  row.querySelector('.include').checked=Boolean(kind);
  row.querySelector('.custom-label').hidden=row.querySelector('.role').hidden=kind!=='custom';
  $(kind?kind+'-samples':'available-samples').append(row);
  show('custom-column',Boolean($('custom-samples').children.length));
  settings();
}
function makeDraggable(row){
  let gesture=null;
  const clear=()=>{gesture=null;row.classList.remove('dragging');for(const col of document.querySelectorAll('.drag-over'))col.classList.remove('drag-over');};
  row.onpointerdown=event=>{
    if(event.button!==0||operationBusy||$('prepare').disabled||event.target.closest('input,select,button,details'))return;
    gesture={id:event.pointerId,x:event.clientX,y:event.clientY,moved:false};row.setPointerCapture(event.pointerId);event.preventDefault();
  };
  row.onpointermove=event=>{
    if(!gesture||gesture.id!==event.pointerId)return;
    if(Math.hypot(event.clientX-gesture.x,event.clientY-gesture.y)>6)gesture.moved=true;
    if(!gesture.moved)return;
    row.classList.add('dragging');const target=document.elementFromPoint(event.clientX,event.clientY)?.closest('.sample-column');
    for(const col of document.querySelectorAll('.sample-column'))col.classList.toggle('drag-over',col===target);
  };
  row.onpointerup=event=>{
    if(!gesture||gesture.id!==event.pointerId)return;
    const target=document.elementFromPoint(event.clientX,event.clientY)?.closest('.sample-column'),moved=gesture.moved;
    if(row.hasPointerCapture(event.pointerId))row.releasePointerCapture(event.pointerId);clear();
    if(moved&&target)moveSample(row,target.dataset.group);
  };
  row.onpointercancel=clear;row.onlostpointercapture=clear;
}
function settings(){if(!scan)return;const controls=selectedRows().some(row=>row.querySelector('.type-select').value==='normal'||(row.querySelector('.type-select').value==='custom'&&row.querySelector('.role').value==='normal'));const ont=mode==='ont',cna=$('analysis').value!=='methylation';$('caller').options[0].disabled=controls;if(controls)$('caller').value='qdnaseq';const ichor=ont&&$('caller').value==='ichorcna';show('controls-note',ont&&controls);show('caller-field',ont&&cna);show('binsize-field',cna);show('reports-field',cna);show('report-context-field',cna&&$('reports').checked);show('methylation-fields',$('analysis').value!=='cna');const old=$('binsize').value,caller=ichor?'ichorcna':'qdnaseq',callerChanged=binsizeCaller!==caller;binsizeCaller=caller;$('binsize').replaceChildren();for(const size of ichor?[500]:binsizes){const option=node('option',String(size));option.value=size;$('binsize').append(option);}$('binsize').value=ichor?'500':(!callerChanged&&binsizes.map(String).includes(old)?old:'100');$('binsize-default').textContent=ichor?'ichorCNA uses 500 kb bins.':'Default: 100 kb for QDNAseq.';const count=selectedRows().length;$('sample-count').textContent=count+' / '+scan.samples.length+' assigned';$('gistic').disabled=count<2;if(count<2)$('gistic').checked=false;$('gistic-note').textContent=count<2?'GISTIC needs at least two assigned samples. CNA and interpretation reports are available for a single sample.':'GISTIC is available for this cohort; leave it off unless recurrence analysis is needed.';invalidate();}
function renderSamples(){
  for(const list of document.querySelectorAll('.sample-list'))list.replaceChildren();show('custom-column',false);
  for(const sample of scan.samples){
    const row=node('article',undefined,'sample');row.dataset.id=sample.id;makeDraggable(row);
    const included=node('input');included.type='checkbox';included.className='include';included.hidden=true;
    const input=node('input');input.value=sample.name;input.className='sample-name';input.setAttribute('aria-label','Sample name for '+sample.name);input.oninput=invalidate;
    const select=node('select');select.className='type-select';select.setAttribute('aria-label','Type for '+sample.name);
    for(const [value,label] of [['','Unassigned'],['normal','Normal'],['cancer','Cancer'],['custom','Custom tag']]){const option=node('option',label);option.value=value;select.append(option);}
    const custom=node('input');custom.className='custom-label';custom.placeholder='Custom sample tag';custom.hidden=true;custom.setAttribute('aria-label','Custom sample type for '+sample.name);
    const role=node('select');role.className='role';role.hidden=true;role.setAttribute('aria-label','Analysis role for '+sample.name);
    for(const [value,label]of[['','Choose analysis role…'],['tumor','Study (tumor workflow group)'],['normal','Control (normal workflow group)']]){const option=node('option',label);option.value=value;role.append(option);}
    select.onchange=()=>moveSample(row,select.value);role.onchange=settings;custom.oninput=invalidate;
    custom.onchange=()=>{const label=custom.value.trim().toLowerCase();if(['normal','control','cancer'].includes(label))moveSample(row,label==='control'?'normal':label);else invalidate();};
    const details=node('details');details.append(node('summary','View paths'),node('pre',sample.files.join('\n')));
    row.append(node('div','⠿ Drag to a group, or choose below','drag-handle'),included,input,node('small',sample.file_count+' FASTQ'+(sample.file_count===1?'':'s')+' · '+(sample.barcode||sample.layout)),select,custom,role,details);
    $('available-samples').append(row);
  }
  settings();
}
$('scan').onclick=()=>busy($('scan'),async()=>{if(!mode)throw Error('Choose a platform first.');scan=await api('/api/scan',{mode,folder:$('input-folder').value});invalidate();$('scan-warnings').replaceChildren();for(const warning of scan.warnings)$('scan-warnings').append(node('p',warning,'notice'));renderSamples();show('samples-card',true);show('settings-card',true);$('samples-card').scrollIntoView({behavior:'smooth'});});
$('select-none').onclick=()=>{for(const row of sampleRows())moveSample(row,'');};$('analysis').onchange=settings;$('caller').onchange=settings;$('reports').onchange=()=>{show('report-context-field',$('reports').checked);invalidate();};$('report-detail').onchange=()=>{$('report-detail-note').textContent=$('report-detail').value==='catalog'?'Uses the included evidence catalog. No publication searches or language-model downloads.':$('report-detail').value==='models'?'Drafts use the bundled CNA catalog and run on this computer. No publication searches. The default model downloads about 8 GB and needs at least 24 GiB available RAM. Sample files are not uploaded.':'Searches publication services using CNA feature terms and study context. Language models run on this computer. The default model downloads about 8 GB and needs at least 24 GiB available RAM. Raw reads and sample names are not sent to publication services.';invalidate();};$('reference').onchange=()=>{show('reference-path-field',$('reference').value==='reuse');$('reference-note').textContent=$('reference').value==='build'?'Local indexing downloads hg38 source files as needed and requires more RAM, temporary disk space, and time.':$('reference').value==='reuse'?'Select a prepared OncoTracer reference parent, or its references/samurai_hg38 folder. It must contain indexes for this platform.':'Prepared downloads are reused. About 8.0 GiB for Illumina or 9.7 GiB for ONT, under your project’s reference folder.';invalidate();};
for(const field of document.querySelectorAll('#settings-card input,#settings-card select'))field.addEventListener('change',invalidate);$('input-folder').onchange=()=>{scan=null;invalidate();show('samples-card',false);show('settings-card',false);if(mode&&$('input-folder').value.trim())$('scan').click();};
$('prepare').onclick=()=>busy($('prepare'),async()=>{if(!scan)throw Error('Discover samples first.');const projectName=$('project-name').value.trim();if(!projectName||projectName==='.'||projectName==='..'||projectName.includes('/')||projectName.includes('\\'))throw Error('Use a new project folder name without slashes.');const samples=selectedRows().map(row=>({id:Number(row.dataset.id),name:row.querySelector('.sample-name').value,type:row.querySelector('.type-select').value,label:row.querySelector('.custom-label').value,role:row.querySelector('.role').value}));if(!samples.length)throw Error('Assign at least one sample to Normal, Cancer, or a custom tag.');for(const sample of samples)if(!sample.type||(sample.type==='custom'&&(!sample.label.trim()||!sample.role)))throw Error('Choose a sample type and, for custom types, an analysis role for every included sample.');if($('analysis').value!=='cna'&&!$('classifier').value)throw Error('Choose a methylation classifier explicitly.');const data={scan_id:scan.scan_id,samples,project:$('project-parent').value.replace(/\/$/,'')+'/'+projectName,analysis:$('analysis').value,threads:Number($('threads').value),backend:$('backend').value,caller:$('caller').value,binsize:Number($('binsize').value),reports:$('reports').checked,report_context:$('report-context').value,report_detail:$('report-detail').value,gistic:$('gistic').checked,reference:$('reference').value,reference_path:$('reference-path').value,classifier:$('classifier').value,methylation_source:$('methylation-source').value,methylation_path:$('methylation-path').value,resources:$('resources').value,device:$('device').value,accept_sturgeon_license:$('license').checked};prepared=await api('/api/prepare',data);$('config-preview').textContent=prepared.config;$('saved-path').textContent='Saved: '+prepared.config_path;$('check-badge').textContent=prepared.valid?'Configuration checked':'Needs attention';$('check-messages').replaceChildren();for(const error of prepared.check.errors||[])$('check-messages').append(node('p',error,'error'));const warnings=prepared.check.warnings||prepared.check.resources?.warnings||[];for(const warning of warnings)$('check-messages').append(node('p',typeof warning==='string'?warning:JSON.stringify(warning),'notice'));$('run').disabled=!prepared.valid;$('job-status').textContent='';show('logs',false);show('review-card',true);$('review-card').scrollIntoView({behavior:'smooth'});});
function offerCleanup(job){
  if(cleanupShown===job.project_id)return;cleanupShown=job.project_id;
  $('cleanup-path').textContent=job.project_path;$('confirm-path').value='';$('confirm-remove').disabled=true;show('remove-confirmation',false);
  $('remove-project').disabled=!job.can_remove;$('cleanup-note').textContent=job.can_remove?'Keep project is the default. Nothing is removed unless you confirm.':'This folder existed before browser setup. Review and remove it manually if needed.';
  $('cleanup').showModal();$('keep-project').focus();
}
async function poll(){
  clearTimeout(pollTimer);
  try{
    const job=await api('/api/status');activeJob=job;if(job.status==='idle')return;
    show('workflow',true);show('review-card',true);$('run').hidden=true;
    const active=['running','stopping'].includes(job.status);
    $('job-status').textContent=job.status==='running'?'Analysis running…':job.status==='stopping'?(job.stop_error||'Stopping analysis…'):job.status==='stopped'?'Analysis stopped':job.status==='removed'?'Project folder removed':job.status==='complete'?'Analysis completed · exit 0':'Analysis failed · exit '+job.exit_code;
    if(job.results_url){$('open-results').href=job.results_url;show('open-results',true);}
    $('logs').textContent=(job.log_truncated?'[Showing the latest log output; full log saved below.]\n':'')+job.log;
    $('log-path').textContent=job.status==='removed'?'':'Full log: '+job.log_path;show('logs',true);
    for(const field of document.querySelectorAll('main button, main input, main select'))field.disabled=true;
    show('stop',active);$('stop').disabled=job.status==='stopping'&&!job.stop_error;
    if(active){pollTimer=setTimeout(poll,1000);}else{$('new-analysis').disabled=false;show('new-analysis',true);if(job.status==='stopped')offerCleanup(job);}
  }catch(error){failure(error);}
}
$('stop').onclick=async()=>{
  if(!activeJob||!['running','stopping'].includes(activeJob.status))return;
  $('stop').disabled=true;
  try{await api('/api/stop',{project_id:activeJob.project_id});await poll();}catch(error){failure(error);$('stop').disabled=false;}
};
$('keep-project').onclick=()=>$('cleanup').close();
$('remove-project').onclick=()=>{show('remove-confirmation',true);$('confirm-path').focus();};
$('confirm-path').oninput=()=>{$('confirm-remove').disabled=$('confirm-path').value!==activeJob?.project_path;};
$('confirm-remove').onclick=async()=>{
  if(!activeJob||$('confirm-path').value!==activeJob.project_path)return;
  $('confirm-remove').disabled=true;
  try{await api('/api/remove-project',{project_id:activeJob.project_id,confirm_remove:true,confirm_path:$('confirm-path').value});$('cleanup').close();await poll();}catch(error){$('cleanup-note').textContent=error.message;$('confirm-remove').disabled=false;}
};
$('new-analysis').onclick=()=>{for(const field of document.querySelectorAll('main button, main input, main select'))field.disabled=false;mode=null;scan=null;binsizeCaller=null;cleanupShown=null;show('stop',false);invalidate();show('workflow',false);show('new-analysis',false);$('run').hidden=false;for(const name of ['ont','illumina'])$('choose-'+name).setAttribute('aria-pressed','false');$('platform-card').scrollIntoView({behavior:'smooth'});$('choose-ont').focus();};
$('run').onclick=()=>busy($('run'),async()=>{if(!prepared)throw Error('Save and review a configuration first.');await api('/api/run',{project_id:prepared.id});await poll();$('run').hidden=true;});
(async()=>{try{if(!token)throw Error('Open the complete URL printed by oncotracer web, including the session code after #.');const system=await api('/api/system');hardware=system.hardware;binsizes=system.qdnaseq_binsizes;$('input-folder').value=system.start_dir;$('project-parent').value=system.start_dir;$('threads').value=system.suggested_threads;$('threads').max=hardware.cpu_workers_available;const ram=bytes=>bytes===null||bytes===undefined?'Unknown':(bytes/1073741824).toFixed(1)+' GiB';for(const [value,label]of[[hardware.cpu_workers_available,'CPU workers available'],[ram(hardware.ram_available_bytes),'RAM available'],[ram(hardware.ram_total_bytes),'RAM total'],[String((hardware.gpus||[]).length),'GPUs detected']]){const item=node('div');item.append(node('strong',String(value)),node('span',label));$('hardware').append(item);}$('gpu-note').textContent=(hardware.gpus||[]).map(gpu=>gpu.name+(gpu.memory_total_bytes?' · '+ram(gpu.memory_total_bytes)+' VRAM':'')).join('; ')+' '+(hardware.gpu_note||'CNA analysis uses CPU.');const defaults=system.defaults||{};
if(defaults.mode)choose(defaults.mode);
if(defaults.project){const split=defaults.project.lastIndexOf('/');$('project-parent').value=defaults.project.slice(0,split)||'/';$('project-name').value=defaults.project.slice(split+1);}
for(const [key,id]of[['input_folder','input-folder'],['analysis','analysis'],['threads','threads'],['backend','backend'],['classifier','classifier'],['resources','resources'],['methylation_source','methylation-source'],['methylation_path','methylation-path'],['reference','reference'],['reference_path','reference-path']])if(defaults[key]!==null&&defaults[key]!==undefined)$(id).value=defaults[key];
$('device').value=defaults.gpu?'gpu':'cpu';$('license').checked=Boolean(defaults.accept_sturgeon_license);$('reference').onchange();
if(defaults.mode&&defaults.input_folder)await $('scan').onclick();
await poll();}catch(error){failure(error);}})();
</script></body></html>'''
